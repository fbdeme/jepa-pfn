# Phase 2 baseline (ii): any-cell masking, data-space bar-distribution CE
# = control group isolating the masking-policy effect from the objective
#   effect (compare against Phase 3 JEPA at identical masking)
run_name: p2_anycell
policy: any_cell
seed: 0
steps: 20000
